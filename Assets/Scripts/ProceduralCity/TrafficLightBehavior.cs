using UnityEngine;

/// <summary>
/// TrafficLightBehavior — komponen penanda prefab traffic light.
/// Di-assign ke prefab TrafficLight (dari Model/Road/AddOns) sebagai filter
/// oleh RoadAddOnDecorator.SetTrafficLightPrefabs().
/// Jalan/prosedural tidak membutuhkan behavior runtime — hanya marker.
/// </summary>
public class TrafficLightBehavior : MonoBehaviour
{
}
